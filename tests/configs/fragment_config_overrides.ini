# This config is incomplete, but will specify all the required key
# when combined with fragment_config_base.ini.

[jira]
project_key = TEST
max_retries = 7

[github]
repository = testing/test-repo

[jira:sync]
sync_milestones = False
